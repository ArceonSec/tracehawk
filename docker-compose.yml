services:
  tracehawk:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/output
      - ./.trivy-cache:/root/.cache/trivy

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - tracehawk
